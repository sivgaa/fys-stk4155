Beklager rotet her er et forsøk på oversikt.

Koden for de ulike delene av prosjektet ligger i hver sin mappe.
Figurer ligger i undermappen er de hører hjemme, men de mest interessante figurene er samlet systematisk i appendix.

Rapporten og vedleggene ligger i mappen Rapport

Part a har programmer for uttesting av ulike versjoner av gradient descent med et polynom.
	Part_a_analycal_derivatives har første uttesting, uten autograd i seg.

	Figurerne som er generert her ligger i en undermappe.

	For metodene der det gjort utprøving med ulike metaparamtere,finnes det to versjoner av programmet.
	-en versjon løper over ulike verdier av metaparamterne og lager heatmap
	-en versjon kjører bare med ett sett parametere lager konvergensplott, disse filene heter noe med "konv"
	
	Part_a_autograd har en versjon av funksjonene som bruke autograd stedenfor analytiske radienter og 
	programmer for å teste dette med SGD og SGD med adagrad

Part b har det nevrale nettverket for regresjon i seg. Figurene som er generert ligger sammen med koden.

Pard d og e har det nevrale nettverket for klassifisering seg, og i tillegg fila for logistisk regresjon (part e), 
	da denne er satt opp som et nevralt nettverk uten skjulte lag.

